import React, {useState, useEffect} from 'react'
import { View, Text } from 'react-native'
import common from '../common.style'


const WeeklyPlanScreen = ({ navigation }) => {
  // for the meal plan screen we need each of the mealPlans (daily)
  // get each of the meals in the plan her
  // get the products to provide photos on the Meal page
  const { selectedMealPlans } = route.params
  return (
    <View style={common.screen}>
    </View>
  )
}

export default WeeklyPlanScreen
